spring:
  datasource:
    url: jdbc:mysql://localhost:3306/sealink
    username: root
    password:
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
  thymeleaf:
    prefix: classpath:/templates/
    suffix: .html
  web:
    resources:
      static-locations: classpath:/static/
  security:
    user:
      name: admin
      password: admin

